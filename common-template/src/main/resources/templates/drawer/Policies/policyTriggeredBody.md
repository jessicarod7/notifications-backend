**[{data.context.display_name}]({environment.url}/insights/inventory/{data.context.inventory_id}?from=notifications&integration=drawer)** triggered {data.events.size()} {#if data.events.size() is 1}policy{#else}policies{/if}.
